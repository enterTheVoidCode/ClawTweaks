<Page
    x:Class="XboxGamingBar.GamingWidgetSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxGamingBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300"
    FontFamily="Bahnschrift SemiLight">

    <ScrollViewer x:Name="RootScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid x:Name="RootGrid">
            <!-- All controls in this Game Bar widget-settings flyout are intentionally hidden.
                 The named elements are kept so the code-behind still constructs/syncs them without
                 error; only their container is collapsed so the flyout shows nothing. -->
            <Grid x:Name="MainGrid" Margin="15,15,15,15" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="Row_0_ProgressRing" Height="Auto"/>
                    <RowDefinition x:Name="Row_1_AutoManageRTSS" Height="36"/>
                    <RowDefinition x:Name="Row_2_OverlayMethod" Height="100"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="AutoStartRTSSText" Text="Auto Start RTSS" FontWeight="SemiBold" FontSize="18" Grid.Row="1"
                    TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" Width="250" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <ToggleSwitch x:Name="AutoStartRTSSToggle" IsEnabled="False" OffContent="Disabled" OnContent="Enabled"
                    FlowDirection="RightToLeft" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right" />

                <controls:RadioButtons x:Name="OnScreenDisplayProviderRadioButtons" Header="OSD Method" FontWeight="SemiBold" FontSize="18" Grid.Row="2">
                    <RadioButton Content="Rivatuner Statistics Server" IsChecked="True"/>
                    <RadioButton Content="AMD Software: Adrenaline Edition" IsChecked="False" />
                </controls:RadioButtons>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
